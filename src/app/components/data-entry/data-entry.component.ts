import { Component } from '@angular/core';\nimport { FormBuilder, FormGroup, Validators } from '@angular/forms';\nimport { DataService } from '../../services/data.service';\n\n@Component({\n  selector: 'app-data-entry',\n  templateUrl: './data-entry.component.html',\n  styleUrls: ['./data-entry.component.scss']\n})\nexport class DataEntryComponent {\n  entryForm: FormGroup;\n  loading = false;\n  success: string | null = null;\n  error: string | null = null;\n\n  constructor(private fb: FormBuilder, private dataService: DataService) {\n    this.entryForm = this.fb.group({\n      field1: ['', [Validators.required]],\n      field2: ['', [Validators.required, Validators.email]],\n      field3: ['', [Validators.required, Validators.min(0)]]\n    });\n  }\n\n  onSubmit() {\n    this.success = null;\n    this.error = null;\n    if (this.entryForm.invalid) {\n      this.entryForm.markAllAsTouched();\n      return;\n    }\n    this.loading = true;\n    this.dataService.saveEntry(this.entryForm.value).subscribe({\n      next: () => {\n        this.loading = false;\n        this.success = 'Entry saved successfully!';\n        this.entryForm.reset();\n      },\n      error: () => {\n        this.loading = false;\n        this.error = 'Failed to save entry.';\n      }\n    });\n  }\n}
