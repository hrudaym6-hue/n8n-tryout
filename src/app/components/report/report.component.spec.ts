import { ComponentFixture, TestBed } from '@angular/core/testing';\nimport { ReportComponent } from './report.component';\nimport { DataService } from '../../services/data.service';\nimport { of, throwError } from 'rxjs';\n\ndescribe('ReportComponent', () => {\n  let component: ReportComponent;\n  let fixture: ComponentFixture<ReportComponent>;\n  let dataServiceSpy: jasmine.SpyObj<DataService>;\n\n  beforeEach(async () => {\n    dataServiceSpy = jasmine.createSpyObj('DataService', ['getReports']);\n    await TestBed.configureTestingModule({\n      declarations: [ReportComponent],\n      providers: [\n        { provide: DataService, useValue: dataServiceSpy }\n      ]\n    }).compileComponents();\n    fixture = TestBed.createComponent(ReportComponent);\n    component = fixture.componentInstance;\n    fixture.detectChanges();\n  });\n\n  it('should create report component', () => {\n    expect(component).toBeTruthy();\n  });\n\n  it('should fetch reports', () => {\n    const mockReports = [{title: 'Report A', description: 'Details'}];\n    dataServiceSpy.getReports.and.returnValue(of(mockReports));\n    component.ngOnInit();\n    expect(component.reports).toEqual(mockReports);\n  });\n\n  it('should handle error', () => {\n    dataServiceSpy.getReports.and.returnValue(throwError(() => new Error()));\n    component.ngOnInit();\n    expect(component.error).toBe('Failed to load reports');\n  });\n});
